# Todo example

This optional Node.js CLI is a test target for the orchestration package.

Run `npm test` from this directory. The CLI is `node src/cli.js`; it supports `add`, `list`, and `done`. Keep temporary test data isolated. The optional `briefs/add-delete.md` exercise adds deletion. When this example is inside the package repository, scope changes to `examples/todo-cli/`.
